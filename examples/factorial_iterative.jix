fn factorial(n) {
    let result = 1;
    while (n > 1) {
        result = result * n;
        n = n - 1;
    }
    return result;
}

let val = factorial(10);

print("Value of the factorial is:");
print(val);

return val;
